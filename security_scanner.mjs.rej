--- security_scanner.mjs
+++ security_scanner.mjs
@@ -219,10 +219,9 @@
                 childScanner.requestList = childCapabilities;
                 childScanner.analyze(subprogram);
             }
             else if (path === 'CPU') {
                 if (this.riskMap["MEDIUM"] > this.currentRisk)
                     throw new Error(`SEC_BLOCK: 'CPU' requires MEDIUM risk (Manifest: ${this.manifest.maxRisk})`);
-                this.analyze(args);
             }
             else if (path === 'WORKER') {
                 if (Array.isArray(args[1])) this.analyze(args[1]);
