# NEJY

An interpreter for a simple JSON/YAML language that calls  node modules.

Install the interpreter from NPM the usual way:

npm i @eaftc/nejy -g


This is very early AI-generated code wrangled through some testing.
 
In early May 2026, we'll explain more here.  
